<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Récap de la commande numéro  <%= @order.id %></h1>
    </p> Cette commande a été passée par <%= @user.first_name %> <%= @user.last_name %> le <%= @order.created_at %></p>

<%if @order.images%>
  <%= @order.images.each do |image| %>
    <%= path = "https://www.example.com" + Rails.application.routes.url_helpers.rails_blob_path(image, only_path: true)%>
    <img src="<%=path%>">
  <%end%>
  <%end%>
 
    <p>
     <%= @url %>
    </p>
   
  </body>
</html>